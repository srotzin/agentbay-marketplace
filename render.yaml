services:
  - type: web
    name: hiveagent
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node start.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: HIVEAGENT_HOST
        value: https://hiveagentiq.com
